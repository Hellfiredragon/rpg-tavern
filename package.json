{
  "name": "rpg-tavern",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "DEV=1 bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
